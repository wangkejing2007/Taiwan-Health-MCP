# ===========================================
# Taiwan Health MCP Server - 環境變數配置
# ===========================================
# 複製此檔案為 .env 並根據需求修改

# -----------------------------------------
# MCP 傳輸模式配置
# -----------------------------------------
# 可選值: stdio | streamable-http | sse
#
# stdio            - 標準輸入輸出（Claude Desktop 本地開發）
# streamable-http  - Streamable HTTP（Docker 生產部署，推薦）
# sse              - Server-Sent Events（Colab + Ngrok，向後相容）

MCP_TRANSPORT=streamable-http

# -----------------------------------------
# 網路配置（僅 http/sse 模式使用）
# -----------------------------------------

# 監聽主機（0.0.0.0 允許外部連線）
MCP_HOST=0.0.0.0

# 監聽埠號
MCP_PORT=8000

# HTTP 端點路徑（僅 http 模式）
MCP_PATH=/mcp

# -----------------------------------------
# 常用配置範例
# -----------------------------------------

# 範例 1: Docker 生產部署 (HTTP)
# MCP_TRANSPORT=streamable-http
# MCP_HOST=0.0.0.0
# MCP_PORT=8000
# MCP_PATH=/mcp

# 範例 2: Claude Desktop 本地開發 (STDIO)
# MCP_TRANSPORT=stdio

# 範例 3: Colab + Ngrok (SSE)
# MCP_TRANSPORT=sse
# MCP_HOST=0.0.0.0
# MCP_PORT=8000
